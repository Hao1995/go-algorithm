package main

import (
	"fmt"

	"github.com/Hao1995/algorithm/solution"
)

func main() {
	// a := 2
	// b := 1000000

	fmt.Println(solution.NumSquaredOperations(a, b))
	// times := 10
	// now := time.Now()
	// // fmt.Printf("Final = %v. Spend = %s\n", solution.NumSquaredOperations(a, b), time.Since(now))
	// for i := 0; i < times; i++ {
	// 	solution.NumSquaredOperations(a, b)
	// }
	// fmt.Printf("Old. Spend = %s\n", time.Since(now))

	// now = time.Now()
	// for i := 0; i < times; i++ {
	// 	solution.NumSquaredOperationsNew(a, b)
	// }
	// fmt.Printf("New. Spend = %s\n", time.Since(now))

	test()
}
